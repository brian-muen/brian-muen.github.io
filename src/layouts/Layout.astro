---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
const isHome = Astro.url.pathname === '/';

const social = [
  { href: 'https://x.com/diffferance', label: 'Twitter' },
  { href: 'https://github.com/brian-muen', label: 'GitHub' },
];
---

<!doctype html>
<html lang="en" class:list={['boot', { home: isHome }]}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {description && <meta name="description" content={description} />}
    <noscript>
      <style>
        html.boot .wordmark-line,
        html.boot .content > main,
        html.boot main.home > *,
        html.boot .content > .social { opacity: 1; }
      </style>
    </noscript>
    <link rel="icon" type="image/svg+xml" href="/favicon-9.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet" />
    <title>{title}</title>
    <script is:inline>
      (function () {
        try {
          var t = localStorage.getItem('theme');
          if (t !== 'light' && t !== 'dark') {
            t = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
          }
          document.documentElement.setAttribute('data-theme', t);
        } catch (e) {
          document.documentElement.setAttribute('data-theme', 'light');
        }
      })();
    </script>
  </head>
  <body>
    <div class="content">
      <header class="site-header">
        <div class="wordmark-line">
          <a href="/" class="wordmark">brian wang</a><button type="button" class="theme-toggle" id="theme-toggle" aria-label="Toggle color theme" title="Theme">.</button>
        </div>
      </header>
      <main class:list={[{ home: isHome }]}>
        <slot />
      </main>
      <footer class="social">
        {social.map((s) => (
          <a href={s.href} rel="me">{s.label}</a>
        ))}
      </footer>
    </div>
    <script is:inline>
      requestAnimationFrame(function () {
        requestAnimationFrame(function () {
          document.documentElement.classList.remove('boot');
        });
      });
    </script>
    <script>
      const root = document.documentElement;
      const btn = document.getElementById('theme-toggle');

      function current() {
        return root.getAttribute('data-theme') === 'dark' ? 'dark' : 'light';
      }

      function syncLabel() {
        if (!btn) return;
        const dark = current() === 'dark';
        btn.setAttribute('aria-pressed', String(dark));
        btn.title = dark ? 'Switch to light' : 'Switch to dark';
      }

      function setTheme(next) {
        root.setAttribute('data-theme', next);
        try {
          localStorage.setItem('theme', next);
        } catch {
          /* ignore */
        }
        syncLabel();
      }

      syncLabel();
      btn?.addEventListener('click', () => {
        setTheme(current() === 'dark' ? 'light' : 'dark');
      });
    </script>
  </body>
</html>
